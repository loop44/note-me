export type Inputs = {
  registerName: string;
  registerEmail: string;
  registerPass: string;
  loginEmail: string;
  loginPass: string;
};

export type AuthParams = {
  username?: string;
  email: string;
  password: string;
};
